#board-wrapper
  #board-info
    #board-actions
      = link_to_void 'Start New Game', :class => 'start-game'
    %p#end-game-results
  #board
    - 9.times do |index|
      .space.unselected{:id => "space-#{index}", :'data-space-id' => index}= index